version: "2"
sql:
  - schema: "internal/order/db/migration"
    queries: "internal/order/db/query"
    engine: "postgresql"
    gen:
      go:
        package: "db"
        out: "internal/order/db"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_interface: true